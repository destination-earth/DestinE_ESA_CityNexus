import React from "react";
import "./modelling-actions.scss";

export const ModellingActions = (props) => {
    const {children} = props;
    return (<div className="citynexus-modelling-actions">{ children }</div>)
}
